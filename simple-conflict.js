// Simple conflict test file
function greet(name) {
    return "Hello, " + name;
}

function calculate(a, b) {
    return a + b;
}

const version = "1.0.0";

module.exports = { greet, calculate, version };
